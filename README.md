# Malevolent Shrine App — GitHub Pages Build

Upload every file and folder in this package to the root of:

`RiosHoldings/Malevolent-Shrine-App`

Required structure:

- `.nojekyll`
- `index.html`
- `manifest.json`
- `assets/index-fc0d8ea7.js`

GitHub Pages URL:

`https://riosholdings.github.io/Malevolent-Shrine-App/`

## Security note

The previous compiled JavaScript contained a complete Discord webhook URL.
This build removes it from the browser bundle. Delete/regenerate that webhook
in Discord before using a private server-side proxy for notifications.
